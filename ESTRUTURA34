#include <stdio.h>

/*
    Declare três matrizes quadradas (a, b e c). Então leia ou gere aleatoriamente os
    números nas duas matrizes a e b. Após isso multiplique-as uma pela outra e mostre
    o resultado da multiplicação (a matriz c irá receber o resultado).
    Lembre-se de verificar se as matrizes possuem dimensões compatíveis para a
    multiplicação.
*/

int main()
{

    int matriz_a[3][3]; // Matriz A
    int matriz_b[3][3]; // Matriz B
    int matriz_c[3][3]; // Matriz C

    for (int i = 0; i < 3; i++)
    {
        for (int j = 0; j < 3; i++)
        {
        }
    }
}
